# Required — from https://portal.cdp.coinbase.com
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
CDP_WALLET_SECRET=

# Optional — defaults shown
# CDP_NETWORK=base                  # use base-sepolia for testing
# CDP_ACCOUNT_NAME=openclaw-default # change to run multiple isolated wallets
# BASE_RPC_URL=                     # override default public Base RPC
